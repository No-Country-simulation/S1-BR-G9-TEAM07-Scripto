# Model package

The integrated runtime expects the trained package at:

`models/scripto-model-v3`

For a lightweight source-only distribution, copy the `scripto-model-v3` directory generated by `artifacts-v2.zip` into this folder before running the backend, or set `SCRIPTO_MODEL_PATH` to its absolute path.
